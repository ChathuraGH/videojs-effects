# Contributing

- Fork and create a feature branch.
- Make changes under `src/`. Copy to `dist/` or run `npm run build`.
- Ensure `index.html` demo works locally.
- Open a pull request with a clear description.